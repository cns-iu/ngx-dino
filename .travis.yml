language: node_js

node_js:
  - "10"

sudo: false

cache:
  directories:
    - "node_modules"

branches:
  only:
    - master
    - develop
    - /^v\d+\.\d+(\.\d+)?(-\S*)?$/

script: skip

jobs:
  include:
    - stage: run tests
    - script: echo "TODO - Testing"
    - stage: deploy demo
      if: branch = master
      script: npm run build:release
      deploy:
        provider: pages
        skip-cleanup: true
        github-token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
        keep-history: false
        local-dir: dist/dino-demo
        on:
          branch: master
    - &npm-publish
      stage: npm publish libraries
      if: branch = /^v\d+\.\d+(\.\d+)?(-\S*)?$/
      script: npm run build:lib core
      before_deploy:
        - cd dist/core
      after_deploy:
        - cd ../../
      deploy: &npm
        provider: npm
        email: $NPM_EMAIL
        api_key: $NPM_TOKEN
        on:
          tags: true
          repo: cns-iu/ngx-dino
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib vega-util
      before_deploy:
        - cd dist/vega-util
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib datatable
      before_deploy:
        - cd dist/datatable
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib force-network
      before_deploy:
        - cd dist/force-network
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib vega-util
        - npm run build:lib geomap
      before_deploy:
        - cd dist/geomap
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib scatterplot
      before_deploy:
        - cd dist/scatterplot
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib science-map
      before_deploy:
        - cd dist/science-map
    - <<: *npm-publish
      script:
        - npm run build:lib core
        - npm run build:lib legend
      before_deploy:
        - cd dist/legend
